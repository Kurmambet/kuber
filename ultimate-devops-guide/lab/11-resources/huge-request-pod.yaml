apiVersion: v1
kind: Pod
metadata:
  name: huge-request
spec:
  containers:
    - name: nginx
      image: nginx:1.25
      resources:
        requests:
          cpu: "100"
          memory: "100Gi"
        limits:
          cpu: "100"
          memory: "100Gi"
